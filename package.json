{
  "name": "my-monitor",
  "version": "1.0.0",
  "description": "monitor",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --no-open --no-notify --files '**/*.json,**/*.css,**/*.js,**/*.html'"
  },
  "keywords": [
    "monitor"
  ],
  "author": "cyy",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.23.6"
  }
}